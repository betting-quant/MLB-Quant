"""Modeling boundaries for workload, strikeouts, and outs recorded.

Submodules (e.g. ``mlb_quant.models.tournament``) are intentionally not
imported here, so that importing this package or ``mlb_quant.models.cli``
never pulls in sklearn/pandas/joblib as a side effect.
"""
